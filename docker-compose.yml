services:
  jobfinder:
    build: .
    ports:
      - "8085:8085"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
